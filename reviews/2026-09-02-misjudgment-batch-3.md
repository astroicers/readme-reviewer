# 第三次誤判批次處理(2026-09-02,11 條)→ rubric 0.4.0 / 工具 0.4.0

> 觸發:B1 真實使用交出 3 條誤判,加上停放的 8 條實驗室縫達門檻。
> **11 修 0 不修**——真實 3 條全是可實測的 code bug;實驗室 8 條全數有
> R2 盲判分裂實據 + B1 活化證據,查證早已完成。

## 真實使用 3 條(code)

| 縫 | 修法 | 驗證 |
|---|---|---|
| emoji slug 假陽性 ×23 | `github_slug` 移除字元後**不再 strip**(GitHub 不 re-trim);`📖 概述` → `-概述` | backup-worker 23→**0**;突變 1 轉紅 |
| S-001 兩形全盲 | `PIPE_TO_SHELL` 補行程替換形 `bash <(curl…)` 與 PowerShell `irm\|iex` 形 | AI-SOP 0→**7 命中**;正反夾具;突變 2/3 轉紅 |
| static 假 badge | scope 明定 badge 事實不進 R-004;動態 badge 才是驗證方式,`/badge/` static 是寫死數字扮 badge | 條文 |

## 實驗室 8 條(criteria,逐縫)

| 縫 | 修法 |
|---|---|
| 1 形狀表缺列+artifact 相撞 | **文件前門型**新列(kernel 型);artifact 規則**限縮**為「禁由 README 薄厚反推品質形狀」,正典文件在樹內的分流 README 是 artifact 的正確描述 |
| 2 載重統計判別法 | 明定三類:歷史完成式(+日期/連結)**不算**;社會證明統計**算**;`free` 對商業品**算**、對倉內 LICENSE 同步之開源品不算。序 2 補「證據欄必列裸露清單」 |
| 3 宣稱即陳述 | **合體裁定**:前置需求句(requires/tested with)視為適用邊界陳述本身,版本字面不二次計費(fiber 格單一出口 → good) |
| 4 badge scope 互拉 | badge 事實整體移出 scope(與真實 #3 同修);httpie 型 → 序 4 n/a 單一出口 |
| 5 delegation 缺立場 | 補應用/索引立場:零指令但 **repo 內檔案級路由** → 視同達成;外鏈路由 → mixed(linux 單一出口 good、dbt 維持 poor) |
| 6 library 豁免 vs 缺件 | 明定:豁免不涵蓋**旗艦範例照貼即缺件**(choo 格裁定 mixed 側——依機制「第一次失敗在第一個範例」,推翻兩位判讀者的多數實踐,理由記此) |
| 7 R-003 連言+清單門檻 | 連言缺一子件=mixed、皆缺=poor、薄非缺記 findings;清單描述「抽 10 見 2 缺且非自明」判別法 |
| 8 R-005 管道構成+href 匿名 | 管道=**能發起雙向對話**的入口;貢獻指南/廣播帳號不算;名字只活在 href 同不算主體 |

R-001 渲染規則補 tagline 歸屬(縫 9a);序號必記與序 1 空缺格 0.3.0 已修(9b/9c),本批僅承接。

## 誠實聲明

- 縫 3/4/5 的「單一出口」推導**有污染**(作者讀過全部盲判值);驗證只能靠之後的真實使用
- 縫 6 是**條文裁定推翻多數判讀實踐**(2 good vs 1 mixed)——依據是機制不是投票,如實記
- 清單門檻「抽 10 見 2」是**選的**判準常數(與 H-004 的 70% 同性質),非量出
- 依裁定,**不再開實驗室盲判驗證本批**;下一批誤判仍等真實使用

## 驗證

selftest / evals / resimulate / blind_agreement ×2 全綠(歷史 EXPECT 凍結不動);
3 個 code 突變 3/3 轉紅;backup-worker 與 AI-SOP 實測前後對照在案。
